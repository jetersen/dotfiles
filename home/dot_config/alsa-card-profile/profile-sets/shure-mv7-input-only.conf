# Expose only the microphone, not the MV7+ headphone output.
[General]
auto-profiles = yes

[Mapping mono-fallback]
device-strings = hw:%f
channel-map = mono
paths-input = analog-input
direction = input
priority = 100
