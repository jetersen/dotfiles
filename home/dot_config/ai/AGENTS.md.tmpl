Repositories live directly under `~/git`. Search matching local repositories before package caches or decompiled dependencies. Treat other repositories under `~/git` as read-only unless asked to change them.

## Working style

- Follow the nearest repository instructions. Work autonomously when the outcome and scope are clear. Resolve minor uncertainty with read-only inspection or a stated assumption. Ask only when the answer could change the result or discard meaningful work.
- Read existing code and tests before editing. Keep scope tight, fix root causes, and use generators instead of editing generated output.
- Verify APIs, flags, and defaults in local source or primary documentation. Add regression coverage, run checks in proportion to risk, and report exactly what ran, including each affected machine or synchronized checkout.
- Verify exact targets before destructive cleanup. Once equivalent cleanup is approved, do not ask repeatedly.
- Keep updates concise and flag findings that change the plan.
- Ask before publishing, pushing, or making external changes unless already authorized.

## Writing style

Apply these rules to all user-facing prose without narrating routine style checks:

- Use plain, direct language.
- Remove filler, puffery, canned chatbot phrases, and sycophantic openings.
- Avoid em dashes, excessive headings, boldface, and forced lists.
- Prefer active voice and concrete claims.
- Vary sentence length and preserve a natural human voice.

{{ output "pass-cli" "item" "view" "--vault-name" "Work" "--item-title" "AI Work Repository Instructions" "--field" "note" | trim }}

## CodeGraph

In repositories with `.codegraph/`, use CodeGraph first for symbol discovery, call paths, implementations, callers, tests, or blast radius. Use `rg` for known-file searches and small text checks.

- When available, call `codegraph_explore` with exact symbols, files, or a focused question. It returns current line-numbered source and call paths, including dynamic dispatch. Load a deferred tool by name through tool search.
- From the shell, run `codegraph explore "<symbol names or question>"`.

Check results against current source and relevant builds or tests. External, generated, reflection-based, or dependency-injection relationships may be missing. Without `.codegraph/`, use the normal search tools.
