{
  # TODO: Replace this empty module with rocket's reviewed disko.devices layout.
  # The machine is intentionally not installable until its stable disk IDs,
  # encryption choice, filesystems, and swap/hibernation needs are known.
}
